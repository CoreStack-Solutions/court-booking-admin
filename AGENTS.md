# Project

- Do not use conventional commit messages. Use linux style: `scope(other): desc`.
- Always use pnpm
- Follow/respect the shadcn CSS color tokens. Never hardcode colors in the app
- Only create new color tokens when the existing ones are not enough
